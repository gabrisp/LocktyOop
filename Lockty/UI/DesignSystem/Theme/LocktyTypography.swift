import SwiftUI

enum LocktyTypography {
    static let largeTitle = Font.system(.largeTitle, design: .default, weight: .light)
    static let title = Font.system(.title3, design: .default, weight: .regular)
    static let headline = Font.system(.headline, design: .default, weight: .regular)
    static let body = Font.system(.body, design: .default, weight: .light)
    static let callout = Font.system(.callout, design: .default, weight: .light)
    static let caption = Font.system(.caption, design: .default, weight: .light)
}
